# Variáveis para deploy no Droplet (docker-compose atrás do Traefik)
# Copie para .env na pasta do projeto no servidor: /opt/dashboard_clinic
#
# Substitua SEU_GITHUB_OWNER pelo dono do repositório no GitHub (user ou org).

IMAGE=ghcr.io/SEU_GITHUB_OWNER/dashboard_clinic:latest
